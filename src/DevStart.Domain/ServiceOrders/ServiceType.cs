namespace DevStart.Domain.ServiceOrders
{
    /// <summary>A one-time paid service sold by the platform as its own service (SC-49).</summary>
    public enum ServiceType
    {
        ScoringReport = 0,
        TermSheet = 1,
        Promotion = 2,
    }
}
